---
name: assuredloop-deliver
description: Deliver an explicitly assigned Issue through scoped implementation, independent tests and review, authorized integration and verified completion.
---

# Deliver assigned work

Consumer: guwenqing/chatgpt-bot-kit. Product context: openspec. Framework: assuredloop-base@0.1.0.

Resolve the selected installation and consumer context through the package README. Read the assigned Issue, original request, applicable consumer requirements/Design, plan items or justified no-Spec basis, dependencies and current authorization. Use contracts/github-work-traceability/spec.md, contracts/review-and-validation/spec.md and contracts/workflow-self-evolution/spec.md, with templates/README.md.

Authoritative entry in that installation: contracts/review-and-validation/spec.md. Shared record/format instructions are in templates/README.md.

Assigned implementation uses Task/deliver, or Bug/deliver when restoring accepted behavior; document-only implementation is still delivery work. Apply the consumer category/activity contract in templates/README.md; classification describes the outcome, not the assignee.

## Confirm scope before editing

Inspect repository instructions, status and existing solutions. Preserve unrelated work and effective signing policy. Limit edits to the assigned outcome; follow explicitly supplied ordering and verify prerequisite delivery rather than trusting a closed label.

Reassess an implementation-only/no-Spec claim. A follow-up can use existing requirements and prior delivery without an active change or new Proposal. Do not rewrite an archived plan. Missing relevant Specs can be legitimate only with an explicit justified reason. A changed guarantee or material design decision returns to planning/owner assessment even if the patch is small. Active-plan additions return to that plan's owner; a new ticket does not authorize expansion.

For a follow-up with no active change, omit the optional change field in actual records; do not copy a prose absence marker into it as a fictional change name. Use only the plan references and execution dependencies actually supported by the handoff, following templates/README.md.

For a PR, resolve the actual destination's current pre-change config/policy and applicable owner decisions. Candidate config, a stale merge-base or a record's arbitrary ref cannot authorize itself. In explicitly authorized initial bootstrap work, follow the evidenced native/manual handoff and label its limitations; do not fabricate activation/configuration or silently waive unavailable policy.

## Verify and implement

For executable work the action order is: independent test author, observed behavioral RED, corresponding implementation, the same tests GREEN, relevant controlled faults, restoration/recheck, and independent review. Do not put implementation before the failing-test step in the concrete handoff plan.

For executable work, use a test author distinct from the implementation producer. Give that author original requirements and observable cases, not the implementation as an oracle. Run applicable tests before the corresponding implementation; inspect and record a genuine failure caused by the missing/wrong behavior. Tool/setup failures are not behavioral RED.

Implement the smallest coherent change, then rerun the same tests to GREEN. Do not disable assertions or adjust expectations to fit faulty code. An incorrect test returns to its independent author with the requirement evidence. Use relevant controlled product-code/input faults to check detection, record their meaning and limits, restore all deliberate faults and rerun the applicable checks. A surviving non-equivalent fault is a verification gap to resolve. Non-executable work instead receives suitable artifact checks and independent semantic review.

Keep full logs outside tracked source. Native tasks.md marks describe candidate contributions, not approval or merged delivery. Do not mark future merge, post-merge verification or Issue closure as already done.

## Review, integrate and complete

Obtain a different eligible agent/session's full-scope internal review under accepted project.review rules. Exclusions and resolved aliases apply; no guessed vendor tiers or self-acceptance. Supply original basis, actual changes, negative cases, tests/fault evidence and limitations. Use an additional/external reviewer when required or explicitly requested. Missing/interrupted reports and stale results remain incomplete.

Create/update the Issue-linked work PR with the authoritative Workflow context and actual evidence. Resolve findings or obtain an explicit accepted disposition; discuss evidence with the reviewer and return material disagreements to the responsible owner. New content or material head/base/policy/work-context changes require renewed applicable assessment. Never transfer an old PASS silently.

After checks/reviews and the actual required approvals cover the exact candidate/destination, perform authorized submission/merge without inventing another approval category. Respect protections and explicit owner holds. Recheck freshness immediately before integration and verify the real merge/squash commit and assigned outcome afterwards. Staging acceptance proves only its assigned staging scope; final integration needs its own assessment.

Record delivery evidence and close the Issue only when its whole assigned scope is delivered. Partial PR contributions leave it open. Do not close the parent or perform unassigned synchronization/archive. Return the result and remaining limits to the current work owner using explicit handoff context.
